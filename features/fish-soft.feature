Feature: We can process our chunks
  Once a ffish and its chunks are define
  I needed to process the chunks.
  # i have no idea how to test this

  # fetch ? gzip?
  # test single phase
  # test compound phase
